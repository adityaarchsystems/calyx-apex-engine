# **SPRINT 28 REALIGNMENT BLUEPRINT: CALYX PROFILE MATRIX**

### **HARDENING PASS: ENVIRONMENT CLEANUP, SYSTEM REALISM, & PRODUCTION ENGINE OVERHAUL**

### **ACTIVE DRIVE FRAMEWORK: D:\\Calyx Profile Matrix**

## ---

**🛑 SECTION 1: CRITICAL CONTEXT PURGE & REPO BOUNDARY LOCK**

Before authorizing any active code generation or file writing steps, the multi-agent network must execute an absolute cache and context flush. All variables, pathways, and scaffolding concepts relating to external system drives (such as D:\\Calyx Studios OS, 300\_LABS, 400\_SHUTTLE, or Calyx\_Core\_Functional\_Agent\_Memory.md) are completely dropped.

The active execution boundary is strictly locked within the directory frame of our monorepo: **D:\\Calyx Profile Matrix**. No tools may read or write outside this boundary.

## ---

**⚡ SECTION 2: ANTI-FRAUD REALIGNMENT SYSTEM MATRIX**

To address the cosmetic visual fakes and fragile slot validation traps exposed during our system audit, the codebase will be refactored using these explicit reality-hardening vectors:

| Architectural Module | Insufficient State (The Gimmick) | Target Production Posture (True Utility) |
| :---- | :---- | :---- |
| **Website Builder Engine** | Drag-and-drop node placement inside fragile, absolute-coordinate slots that trigger layout breaking alert blocks. | Unmount React Flow grid entirely in website mode. Render an integrated fullscreen tab containing a **Responsive Live Document Preview Sheet** driven by a sidebar **Structural Customizer Control Panel**. |
| **Kinetics Motion Track** | Decorative sidebar toggle switches that execute zero live visual rendering updates on active canvas cards. | Map toggle context metrics directly to the state registry. Inject native SVG proxy-durable CSS @keyframes and SMIL animation rules into backend compilers. |
| **Infrastructure Telemetry** | A client-side Math.random() script loop that forces shifting line coordinates upon clicking the test cloud route button. | Fire a genuine asynchronous network fetch handshake call to a stateless performance timer endpoint (GET /api/v1/health/ping) calculating real millisecond lag. |
| **Iconography Registry** | Lacks built-in technology logo groupings, risking horizontal subpixel layout line bleeding across screens. | Deploy a centralized **Dual-Tier High-Fidelity Iconography Dictionary** supporting original solid-brand arrays and uniform minimalist monochrome wireframes. |

## ---

**🛠️ SECTION 3: DECENTRALIZED COMPONENT TARGET SCOPES**

To guarantee absolute monorepo decoupling safety, assignments are partitioned across our dedicated application sub-agent streams in this chronological file modification sequence:

\[SYS-ARCH: Packages/Types\] ──\> \[DEPL-ENG: Sync-Worker Routes\] ──\> \[SYNC-MON: Edge Image Server\] ──\> \[UI-ENG: Web-Canvas Studio\]

### **1\. SYS-ARCH (Sub-Agent — Type Contract Normalization)**

* **Target Scope:** packages/types/src/index.ts  
* **Engineering Directive:** Overwrite shared layout type definitions under a purely declarative posture using strict 4-space code indentation layout. Define object models for WebsiteSectionConfig, KineticAssetRegistry, and IconographyTierConfig. Purge legacy absolute position coordinate values from website stack models.

### **2\. DEPL-ENG (Sub-Agent — Stateless Serverless Automation & Telemetry)**

* **Target Scope:** apps/api-sync-worker/src/index.ts  
* **Engineering Directive:** Enforce strict stateless V8 isolate compatibility constraints—completely eliminate local file system disk paths. Implement the operational endpoint GET /api/v1/health/ping using high-resolution performance timers (process.hrtime()). Stream genuine connection metrics back to client listeners via Supabase real-time broadcast channels (sync\_health\_stream).  
* **Security Architecture Gating:** Secure the form capture endpoint POST /api/v1/forms/capture/:userId using an invisible bot honeypot tracking input tag (name="\_calyx\_hp"). Pair this layout defense with an atomic token-bucket rate limiter verification check utilizing Redis key tracking to protect against automated spam loop cycles:

$$T\_{\\text{available}} \= \\min\\left(B, T\_{\\text{last}} \+ r \\cdot \\left(t\_{\\text{now}} \- t\_{\\text{last}}\\right)\\right)$$

### **3\. SYNC-MON (Sub-Agent — Edge Asset Drawing & Vector Micro-Injections)**

* **Target Scope:** apps/edge-asset-server/src/utils/svgCompiler.ts  
* **Engineering Directive:** Overhaul the edge SVG vector compiler module. Implement vector drawing macros to output the new ContributionsMatrix bento block as native SVG rectangle layouts. Map the dual-tier tech icon dictionary matrices natively (MAX\_FIDELITY\_STANDARD vs. MAX\_FIDELITY\_MINIMALIST) based on chosen configuration style properties.  
* **Animation Proxy Routing:** Process Tier 4 KineticMediaBox components by wrapping media loops safely within standalone SVG \<image\> tag elements. This ensures transparent .gif and .webp graphics pass flawlessly through the content filter properties of GitHub's secure Camo Cache proxy system. Eliminate typography line drift inside vector biographies by replacing relative text offsets (dy) with absolute coordinates combined with dominant-baseline="central" alignments.

### **4\. UI-ENG (Sub-Agent — Client Studio Viewports & Interaction Finesse)**

* **Target Scope:** apps/web-canvas/src/...  
* **Engineering Directive:** Re-engineer context handling inside StudioCanvas.tsx and CanvasContext.tsx. Shifting editing states must completely flush out active node list drawings (setNodes(\[\])) and isolate standard blocks from website configuration lists. Build the fullscreen tabbed website customizer layout viewpane driven entirely by sidebar inputs. Clamp the cache telemetry sparkline graphs to exactly **85%** of the bento card container's height to resolve text clipping anomalies. Isolate node button clicks using absolute pointer event cancellation loops (e.stopPropagation()), and map unstyled browser alert flags to custom inline sliding micro-toast panels matching Calyx Studio styling tokens.

## ---

**🧪 SECTION 4: PRODUCT PRODUCTION SIGN-OFF CHECKS**

Following codespace asset modifications, the platform system configuration must evaluate cleanly against these rigorous quality checkpoints:

1. **Concurrent Workspace Compile Check:** Executing a root-level pnpm build task command sequence must compile all packages concurrently under Turborepo caching matrices with a **100%** successful green compilation trace with zero module boundary leaks or broken type safeties.  
2. **Airtight State Sandboxing Audit:** Confirm that switching editing modes completely flushes active node arrays, protecting adjacent workspaces from data bleeding. Verify that website layout container views lock graph coordinates, overlaying responsive layouts with absolute zero slot compatibility drop failures.  
3. **True Infrastructure Telemetry Attestation:** Inspect network activity streams when clicking the test cloud route button. Confirm that a genuine asynchronous fetch call hits the sync worker's latency ping route, computing true infrastructure lag down to millisecond accuracy with zero random fakes.  
4. **Proxy Evasion Verification:** Query the edge asset compiler to verify that output SVG code strings securely embed raw technology icon paths and incorporate native \<image\> tags that pass transparent looping assets cleanly through GitHub's Camo Cache proxy filters without vertical line drift defects.

### ---

**🛑 SYSTEM REALIGNMENT SPECIFICATION SCALED: STANDBY MODE ACTIVE**

The target paths are verified, the fakes are fully unmasked, and the multi-agent network is constrained within strict monorepo bounds. Standing down and holding the prompt engine loop right here. Ready to generate the active, production-grade codebase modification instructions on your direct command.