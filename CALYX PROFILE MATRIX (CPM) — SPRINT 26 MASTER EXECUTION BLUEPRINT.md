# **CALYX PROFILE MATRIX (CPM) — SPRINT 26 MASTER EXECUTION BLUEPRINT**

### **ENGINE RUNTIME STAGE: ARCHITECTURAL COMPILATION PHASE**

### **TARGET DESIGNATION: DUAL-ENGINE PORTFOLIO & NO-CODE SITE GENERATOR PLATFORM**

### **SYSTEM POSTURE: PROXY-EVADING KINETICS, AUTOMATED GIT WORKFLOWS, & INLINE VECTOR DICTIONARIES**

## ---

**🌌 SYSTEM OVERVIEW & ARCHITECTURAL MOAT**

This Master Execution Blueprint formalizes the structural, data-contract, and visual parameters required to upgrade the Calyx Profile Matrix (CPM) into a **Dual-Engine Identity Platform**. The system will concurrently drive:

1. An ultra-dense, proxy-durable, animated markdown profile graphic engine.  
2. An enterprise-grade, no-code Static Site Generator (SSG) that automates the deployment of interactive, full-screen developer websites hosted natively on GitHub Pages (username.github.io).

## ---

**👥 SECTION 1: THE README KINETIC ANIMATION & ICONOGRAPHY ENGINE**

To bypass the structural constraints of GitHub's markdown parser and the caching barriers of the Camo Cache proxy system, all animations, transitions, and brand assets will be delivered using self-contained vector kinetics and inline caching headers.

\+-----------------------------------------------------------------------------------+  
|                            EDGE ASSET SERVER DELIVERY                             |  
|  \+-------------------------+  \+-------------------------+  \+-------------------+  |  
|  |   Inline Icon Mapping   |  |   CSS Keyframe Influx   |  |  Short-TTL Cache  |  |  
|  | (Simple Icons / Lucide) |  |   (@keyframes loops)    |  |  Control Headers  |  |  
|  \+------------+------------+  \+------------+------------+  \+---------+---------+  |  
\+---------------|----------------------------|-------------------------|------------+  
                v                            v                         v  
\+-----------------------------------------------------------------------------------+  
|                             GITHUB CAMO CACHE PROXY                               |  
|                     \[ Passes Sanitized SVG Natively to Viewport \]                  |  
\+-----------------------------------------------------------------------------------+

### **1\. Vector Icon Map Dictionary & Path Injections**

* **The Strategy:** Eliminate all slow, external image source strings (\<img src="..."\>) that break layout alignment or drop frames behind proxies. Build a centralized, highly optimized icon mapping dictionary inside apps/edge-asset-server and apps/web-canvas.  
* **The Implementation:** Embed raw SVG \<path\> definitions natively for technology metrics (e.g., React, Rust, Go, Tailwind, Kubernetes) and semantic Lucide markers. On compilation, the image server injects the raw paths directly into component layout grid boundaries using exact typographic layout controls (dominant-baseline="central").

### **2\. The Narrative Primitive Card (About/Bio Node)**

* **Dimensional Footprint:** A dedicated structural card spanning exactly $320\\text{px} \\times 128\\text{px}$ (h-32 w-80) with options for variable row stretching inside the editor palette.  
* **Hydration Pipeline:** The crawler automatically parses the user's public biography text string during the primary GitHub GraphQL query cycle. If the string is missing or empty, it dynamically falls back to an open-source developer archetype text segment.  
* **Word-Wrapping Engine:** To shield text blocks from bleeding past card container lines inside static vector images, strings are split cleanly at word boundaries and compiled inside stacked, relative \<tspan\> sub-elements. The initial segment resets relative offsets to zero (dy="0"), while subsequent blocks wrap downwards on precise relative text steps (dy="1.2em").

### **3\. Passive Kinetic Visual Matrix (Zero-Click Animation)**

Inject high-density motion directly inside the sandboxed image vectors, forcing the user's browser rendering layer to execute animation loops locally within the profile viewport:

* **The Infinite Terminal Node:** Integrates a continuous typing carousel animation paired with a ticking console cursor block driven by native inline CSS animation parameters (@keyframes typingEffect and @keyframes pulseCursor).  
* **The Telemetry Wave Node:** Upgrades static analytics line charts into smoothly flowing wave functions, mapping coordinate point mutations natively via embedded SVG SMIL transformation tags (\<animateTransform\>).  
* **Micro-Glow Shifting Ambient Borders:** Applies smooth container breathing cycles (@keyframes edgeGlow) to card border structures to establish visual depth under dark viewports.

## ---

**🌐 SECTION 2: GITHUB PAGES NO-CODE STATIC SITE GENERATOR (SSG)**

This engine acts as a standalone portfolio website deployment platform, allowing first-time users to compile and host highly interactive, full-screen websites on their personal domain with zero command-line configuration.

### **1\. Premium Interactive Web Templates**

| Web Template Variant | Target Developer Track | Key Interactive Visual Mechanics |
| :---- | :---- | :---- |
| **LUXURY\_GLASSMORPHISM** | Web Stacks / UI/UX Track | Mouse-tracking parallax movement layers, organic fluid background gradient blobs, backdrop filters (backdrop-blur-xl), and smooth modal popovers for deep project case studies. |
| **RETRO\_TERMINAL** | Systems / Backend Track | A functional, text-driven UNIX terminal simulator viewport. Visitors can type live commands (help, projects, clear, contact) into a javascript-backed terminal shell state machine. |
| **SCANDI\_MINIMALIST** | Designer / Founder Track | High whitespace breathing ratios, stark typographic size adjustments, seamless dark/light mode baseline toggles, and smooth fluid element transitions during routing states. |

### **2\. Streamlined Git Infrastructure Provisioning Workflows**

To completely shield users from manual repository initialization, SSH key mapping, or local terminal tasks, the synchronization worker implements three automated distribution paths:

* **Avenue A: Direct Content Commits API (Zero Setup Deployment)**  
  * The sync worker compiles the user's chosen web layout configurations into a single, highly optimized, production-ready static production folder (index.html, minified stylesheets, inline interactive scripts). It ships these assets directly to the master branch of their username.github.io repository via authenticated PUT /repos/{owner}/{repo}/contents/{path} base-64 payload injections, activating the site instantly without compilation queues.  
* **Avenue B: GitHub Actions CI/CD Blueprint (Automated Self-Hydration)**  
  * The engine provisions a custom automation workflow file inside the target repository at .github/workflows/cpm\_self\_hydrate.yml. Driven by a nightly cron schedule trigger or a webhook alert from our platform, this action awakens, requests updated development data from our sync worker, regenerates a static portfolio snapshot, and commits the code updates completely hands-free.  
* **Avenue C: Serverless Form Relay Infrastructure**  
  * To resolve the static hosting form-processing bottleneck, CPM deploys a stateless contact relay pathway (POST /api/v1/forms/capture/:userId). Premium templates embed an out-of-the-box contact panel that routes message submittals straight to our worker, forwarding alerts to the user's Room D console logs pane in real-time.

## ---

**🛠️ SECTION 3: CORE DATA DEDUPLICATION & BACKUP FALLBACKS**

To protect layouts from data redundancy and component collapse under light profile activity states, the system enforces strict structural layout validation rules.

### **1\. Column-Aware Unique Metrics Allocator Loop**

When a profile sync command executes, the slot-allocation matrix algorithm maps metrics contextually. If an active metric configuration property key (e.g., totalCommitContributions) has already been claimed by a prominent canvas bento card block, the allocator automatically marks that key as occupied inside a rolling validation tracker. Any adjacent or secondary statistic tracking cells are forcefully prevented from using that duplicate key, cycling down an internal fallback priority metrics chain:

$$\\text{Priority Chain: } \[\\text{totalStarsCount}\] \\rightarrow \[\\text{totalCommitContributions}\] \\rightarrow \[\\text{openSourceContributionCount}\] \\rightarrow \[\\text{accountCreationDate}\]$$

### **2\. Baseline Brand Indicator Fallback Tiles**

If a user's scraped public profile logs contain insufficient data bytes to fully populate all secondary or trailing bento layout positions, the system avoids generating blank spaces or throwing layout coordinate collapse errors. The engine automatically seeds unallocated canvas slots with pre-configured **Baseline Brand Indicator Tiles** that render core technical ecosystem values (e.g., "CALYX CORE SECURE", "V8 EDGE BRIDGE") framed behind elegant, soft ambient neon glowing border animations matching the active design system layout tokens.

## ---

**📋 SECTION 4: CROSS-WORKSPACE REFACTORING BLUEPRINTS**

To realize these features safely without breaking the monorepo compile boundaries, changes must deploy across files in this exact sequential order:

\[Harden Shared Data Types\] ──\> \[Update Sync Workers / Scrapers\] ──\> \[Refactor Frontend Views\] ──\> \[Overhaul SVG Factory\]

### **1\. Extending Global Ambient Contracts**

* **Target File:** /packages/types/src/index.ts  
  * Overwrite shared typings to formally deprecate old keys, append layout schemas for DeveloperProfileMetrics, and register the configuration interfaces for the new website template metrics and kinetic animation tracks (TemplateDeduplicationConfig, CustomDomainSslConfig). Maintain a strictly declarative posture with zero executable function code in this module.

### **2\. Hardening GraphQL Contributions Scrapers & Git Provisioners**

* **Target File:** /apps/api-sync-worker/src/index.ts  
  * Overhaul the core router crawler engine to inject the global GitHub GraphQL contributionsCollection query signature. Build the extractQuotaMetrics body-aware parsing middleware, mount the Cloudflare-safe fetch DNS-over-HTTPS check routers, and assemble the repository creation and automated commits API pipeline pathways.

### **3\. Deploying Narrative Primitives & Dashboard Interactions**

* **Target File:** /apps/web-canvas/src/components/nodes/CustomNodes.tsx & /apps/web-canvas/src/context/CanvasContext.tsx  
  * Purge legacy placeholder labels; build out the unique metrics allocator loop inside addNode(), register the About/Bio node views, and map the default data matrices for the new template seeding states.  
* **Target File:** /apps/web-canvas/src/components/Dashboard.tsx & /apps/web-canvas/src/components/Integrations.tsx  
  * Suppress code snippet scroll tracks completely using horizontal .scrollbar-none CSS masks. Embed percentage-locked timestamp marker tracks beneath cache sparklines. Wrap validated, active domain string headers inside target-blank hyperlink anchors equipped with native e.stopPropagation() event protection.

### **4\. Overhauling Image Server Drawing Factories**

* **Target File:** /apps/edge-asset-server/src/utils/svgCompiler.ts  
  * Overwrite drawing macros to ingest localized Simple Icons path data strings. Program the vector factories to inject inline CSS keyframe variables (@keyframes pulseGlow, @keyframes typingEffect) directly within output files to pass animations through the Camo Cache natively with explicit absolute layout alignment rules.

## ---

**🧪 SECTION 5: SIGN-OFF QUALITY ASSURANCE PROTOCOLS**

Following codespace modifications, the platform system configuration must evaluate successfully against these production checkpoints:

1. **Concurrent Turborepo Green Trace Command:** Execution of a root-level pnpm build command must concurrently compile all 5 workspace modules under caching matrices with an absolute 0-error, 0-warning compilation trace.  
2. **Kinetic Asset Proxy Evasion Test:** Querying port 3002 must yield valid SVG XML layers containing structured inline keyframe declarations that execute infinite animation cycles natively without loading external script dependencies.  
3. **Data Deduplication Uniqueness Audit:** Initiating an onboarding sync loop for low-activity profiles must output a high-variety canvas configuration where neighboring blocks reflect distinct parameters, filling remaining empty slots cleanly with soft-glowing Brand Indicator tiles.  
4. **Isolate-Safe Automated Git Provisioning Check:** Simulating a website deployment request must confirm that repository creation and file-writing routines operate entirely via stateless asynchronous network fetch tunnels, preserving absolute Cloudflare Worker V8 isolate compatibility.

### ---

**🛑 MASTER BLUEPRINT COMPILED & LOCKED**

This Master Execution Blueprint maps out the complete tactical sequence required to deliver Sprint 26 to strict Calyx Studios standards.

Per your strict system instructions, **I am officially standing down and holding the prompt generator loop right here.** I will not generate or output the active code generation sprint prompt until you explicitly ask me to.

Review this granular blueprint report. Whenever you are ready to authorize the autonomous sub-agent network to translate these blueprints into active codebase transformations on disk, simply give the word\!